<div class="flex items-center justify-between px-6 py-5 border-b border-neutral-200 cart-drawer-header">
  <div>
    <h2 class="text-xl font-semibold">Your Cart</h2>
    <p class="text-sm text-neutral-500 cart-drawer-header-count">{{ cart.item_count }} Items</p>
  </div>

  <button
    id="cart-close"
    class="size-10 rounded-full hover:bg-neutral-100 flex items-center justify-center"
  >
    ✕
  </button>
</div>
